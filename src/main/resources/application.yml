spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fengdeng?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  thymeleaf:
    cache: false

server:
  address : 0.0.0.0
  port: 8080
